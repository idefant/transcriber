# Диктовка и обработка

## Назначение

Диктовка превращает голос пользователя в текст и вставляет результат в текущее активное приложение. Основной запуск происходит через горячую клавишу.

## Запуск и остановка записи

Пользователь настраивает горячую клавишу в разделе настроек. Значение по умолчанию: `Ctrl+Space`.

Поддерживаются два режима:

- `По нажатию`: первое нажатие горячей клавиши начинает запись, следующее нажатие останавливает запись.
- `По зажатию`: запись начинается при нажатии горячей клавиши и останавливается при отпускании основной клавиши комбинации.

Если запись уже идет, повторный старт не создает новую параллельную сессию. В один момент активна только одна сессия диктовки.

## Проверка готовности перед записью

Перед началом записи приложение проверяет, что аудио можно будет обработать: выбраны провайдер и модель распознавания речи, модель доступна для выбранного провайдера, у провайдера задан API-ключ. Если постобработка включена, те же условия проверяются для провайдера и модели постобработки.

Если проверка не пройдена, запись не начинается и оверлей записи не показывается. Вместо этого приложение показывает системное уведомление операционной системы с описанием проблемы: какой раздел настроек требует внимания (распознавание речи или постобработка) и в чём ошибка. Текст причины использует текущий язык интерфейса приложения.

Клик по такому уведомлению открывает главное окно приложения на соответствующей вкладке настроек и закрывает уведомление. Пока проверка не пройдена, отпускание или повторное нажатие горячей клавиши записи не запускают запись и не приводят к ошибке.

Та же проверка выполняется перед повтором последней записи по горячей клавише: если конфигурация не готова, повтор не запускается, оверлей не показывается, и пользователь получает такое же системное уведомление. Повтор записи через кнопки в истории выполняется при открытом главном окне и показывает ошибку конфигурации прямо в карточке записи, без системного уведомления.

Системные уведомления о готовности конфигурации показываются только на Windows.

## Оверлей записи

Во время диктовки приложение показывает окно поверх остальных окон. Внешний вид и размещение оверлея зависят от общих настроек (см. [Настройки, провайдеры и модели](settings-and-providers.md)):

- вид оверлея: компактная панель снизу экрана или большая панель по центру экрана;
- экраны: только тот монитор, на котором находится курсор (если позицию курсора определить не удалось — основной монитор), либо одновременно каждый подключенный монитор.

В режиме «каждый экран» оверлей отображается на всех мониторах синхронно и показывает одинаковое состояние; отменить диктовку можно с любого из них.

Оверлей имеет состояния:

- `Recording` - идет запись с микрофона;
- `Paused` - запись приостановлена; оверлей показывает значок и текст паузы, желтый акцент и не показывает уровень входного сигнала микрофона;
- `Detecting speech` - приложение локально определяет речь и удаляет тишину перед распознаванием; показывается только при включенной настройке удаления тишины;
- `Transcribing` - аудио отправлено на распознавание;
- `Processing` - выполняется постобработка;
- `Error` - запрос завершился ошибкой и текст не был вставлен; оверлей сохраняет базовый фон и показывает красный акцент;
- `Warning` - распознавание успешно и текст вставлен, но постобработка завершилась ошибкой; оверлей сохраняет базовый фон, показывает оранжевый акцент и значок предупреждения;
- скрыт - активной диктовки нет.

Во время записи оверлей показывает уровень входного сигнала микрофона. На оверлее есть кнопка отмены диктовки.

Для варианта «компактная панель снизу» позиция рассчитывается относительно доступной рабочей области выбранного монитора, в которой открывалось бы развёрнутое на весь экран окно. Если положение или размер taskbar меняются, оверлей смещается вместе с нижней границей этой области и сохраняет тот же отступ от неё.

## Уведомления об ошибках на оверлее

При ошибке локальной обработки аудио или распознавания оверлей переходит в состояние `Error` с красным акцентом, при ошибке постобработки после успешного распознавания — в состояние `Warning` с оранжевым акцентом. Базовый фон оверлея в этих состояниях не меняется. Само сообщение об ошибке на оверлее не показывается.

В этих состояниях оверлей:

- держится на экране до 5 секунд и скрывается автоматически, если пользователь не взаимодействовал с ним мышью;
- если пользователь подвигал курсором мыши над уведомлением оверлея до истечения этих 5 секунд, оверлей не скрывается, пока курсор остаётся над уведомлением;
- после ухода курсора с такого уведомления оверлей скрывается либо по исходному дедлайну в 5 секунд с момента показа, либо через 2 секунды после ухода курсора, в зависимости от того, что произойдёт раньше;
- показывает кнопку закрытия, которая скрывает оверлей сразу;
- показывает кнопку открытия записи, если для ошибки была создана запись истории. Открытие записи выводит на передний план главное окно приложения, переходит в историю и открывает детали этой записи.

Начало новой диктовки в течение этих 5 секунд заменяет уведомление обычным оверлеем записи.

В режиме «каждый экран» hover любого экземпляра такого уведомления удерживает все одновременно показанные уведомления, пока курсор не уйдёт с последнего экземпляра, над которым пользователь уже двигал мышью.

## Пауза записи

Пользователь может приостановить запись горячей клавишей паузы. По умолчанию эта горячая клавиша не задана, то есть пауза выключена.

Пауза доступна только в режиме запуска записи `По нажатию`. В режиме `По зажатию` запись идет ровно столько, сколько удерживается горячая клавиша диктовки, поэтому пауза не поддерживается: поле горячей клавиши паузы в настройках заблокировано, а сама горячая клавиша не срабатывает.

Правила:

- поставить запись на паузу можно только пока идет запись; в остальных состояниях горячая клавиша паузы ничего не делает и не блокируется для других приложений;
- одна и та же горячая клавиша ставит запись на паузу и продолжает ее;
- пока запись на паузе, звук с микрофона не записывается, микрофон освобождается, а оверлей переходит в состояние `Paused`;
- если настройка «Возвращать звук во время паузы записи» включена, пауза возвращает системный звук: заглушенный вывод снова звучит, а остановленное воспроизведение продолжается; продолжение записи применяет режим звука заново. Если настройка выключена, звук остается приглушенным до конца сессии, а продолжение записи не ждет остановки воспроизведения, потому что оно и так остановлено;
- продолжение записи возвращает оверлей в состояние `Recording` и дописывает новый звук к уже записанному; время паузы в аудио не попадает, вместо него куски записи разделяет 0,5 секунды тишины, независимо от длительности самой паузы;
- пока приложение ждет остановки воспроизведения перед продолжением записи, оверлей остается в состоянии `Paused`;
- остановка записи горячей клавишей диктовки и отмена диктовки снимают паузу: приостановленная запись отправляется на распознавание вместе с уже записанным звуком, а отмененная - отбрасывается.

## Отмена диктовки

Пользователь может отменить диктовку через кнопку на оверлее или через горячую клавишу отмены.

Горячая клавиша отмены перехватывается системно только пока сессия диктовки активна — с момента начала записи до завершения постобработки или вставки текста. Вне сессии клавиша не блокируется и доходит до других приложений в обычном режиме. По умолчанию горячая клавиша отмены — `Escape`. Пользователь может изменить ее в настройках или очистить поле, чтобы отключить горячую клавишу отмены полностью.

Правила:

- если отмена выполнена во время записи, запись прекращается и оверлей скрывается;
- если отмена выполнена во время распознавания или постобработки, текущая сессия помечается отмененной, оверлей скрывается, а результат этой сессии не вставляется;
- отмена в неактивном состоянии ничего не меняет.

## Запись аудио

Приложение использует системное устройство ввода по умолчанию. Если устройств ввода несколько, приложение использует то, которое операционная система в текущий момент считает устройством ввода по умолчанию. Если такое устройство недоступно, запись не начинается и пользователь получает ошибку.

Настройка «Звук при записи» задаёт, что приложение делает со звуком системы на время записи: заглушать вывод, ставить воспроизведение на паузу или не трогать звук. После остановки или отмены записи приложение возвращает звук в прежнее состояние. Если приглушить звук не удалось, запись всё равно начинается.

В режиме «заглушать вывод» приложение при старте записи отключает звук на системном устройстве вывода по умолчанию. Если до начала записи устройство вывода было уже заглушено пользователем, приложение оставляет его заглушённым после записи.

В режиме «ставить на паузу» приложение перед началом записи останавливает воспроизведение и ждёт, пока оно действительно прекратится. Оверлей на это время не появляется: запись ещё не идёт. Если воспроизведение не остановилось за отведённое время, запись начинается всё равно. После записи приложение продолжает только то, что остановило само: если пользователь за время записи сам возобновил воспроизведение или переключил трек, приложение его не перебивает. Режим действует на приложения, которые сообщают системе о своём воспроизведении, например плееры и браузеры; звук игр, звонков и системных уведомлений так остановить нельзя — для них подходит режим «заглушать вывод».

После остановки записи приложение немедленно освобождает микрофон и возвращает системный звук. По умолчанию включена настройка «Удалять тишину перед распознаванием»: приложение локально определяет речь, считает паузу от 1200 мс границей речевых сегментов и сохраняет по 350 мс до и после каждого сегмента. Речь короче 300 мс не считается достаточной для распознавания. Если речь не обнаружена или не достигает 300 мс, приложение не отправляет аудио на распознавание и не создаёт запись истории: сессия молча завершается, оверлей скрывается, сообщение об ошибке не показывается. При внутренней ошибке локальной обработки аудио оверлей, наоборот, переходит в состояние `Error`, и аудио также не отправляется на распознавание. Когда настройка выключена, запись сохраняется целиком. Перед сохранением приложение автоматически выравнивает громкость записи до единого уровня, чтобы тихая речь звучала комфортно при воспроизведении и распознавании, после чего аудио кодируется в WAV. В истории и на диске сохраняется тот же очищенный WAV-файл, который отправляется на распознавание. Если за время записи не было получено аудиоданных, сессия завершается ошибкой с состоянием `Error` на оверлее.

## Распознавание речи

Провайдер и модель Speech-to-Text должны быть выбраны и настроены. Их готовность проверяется до начала записи (см. раздел «Проверка готовности перед записью»): если конфигурация распознавания не готова, запись не начинается и распознавание не выполняется.

Для распознавания используются:

- аудиофайл текущей диктовки;
- выбранная модель;
- выбранный язык: `auto`, `ru` или `en`;
- prompt для Speech-to-Text;
- слова из пользовательского словаря.

Если выбран язык `auto`, параметр языка не передается в запрос распознавания. Если выбран `ru` или `en`, выбранный язык передается в запрос.

## Словарь в распознавании

Слова из словаря объединяются в строку и подставляются в prompt Speech-to-Text. Ожидаемое поведение: модель должна использовать эти написания, когда соответствующие слова встречаются в речи.

## Постобработка

Постобработка выполняется только если она включена в настройках.

Перед постобработкой должны быть выбраны провайдер и модель постобработки. Если постобработка включена, но ее конфигурация неполная или запрос постобработки завершился ошибкой, после успешного распознавания этап постобработки завершается ошибкой; итоговая запись истории сохраняет успешную транскрипцию и ошибку постобработки, а текст распознавания всё равно вставляется.

Цель постобработки - очистить распознанную речь:

- убрать слова-паразиты и случайные повторы;
- исправить очевидные ошибки распознавания, грамматику и пунктуацию;
- сохранить смысл, тон и язык говорящего;
- не выполнять команды, вопросы или инструкции, продиктованные пользователем;
- не добавлять пояснения, метки, преамбулы или ответ на продиктованный текст;
- вернуть только очищенный текст.

Если постобработка отключена, итоговым текстом считается результат распознавания.

## Вставка результата

После успешного завершения диктовки приложение вставляет итоговый текст в активное приложение через буфер обмена и команду вставки. Данные помечаются как исключённые из истории буфера обмена Windows, поэтому не появляются в Win+V и облачной синхронизации. После вставки буфер обмена возвращается к содержимому, которое было в нём до диктовки: восстанавливается текст, растровые изображения, списки файлов, HTML и прочие форматы, которые Windows хранит как блок памяти. Если буфер был пуст, он остаётся пустым. Таким образом, Ctrl+V после диктовки вставляет то, что было в буфере до начала диктовки.

Не восстанавливаются форматы, представленные дескрипторами GDI, а не памятью: векторные метафайлы (`CF_METAFILEPICT`, `CF_ENHMETAFILE`) и приватные форматы приложений. Растровые изображения при этом не страдают: Windows пересоздаёт их из сохранённого `CF_DIB`. Содержимое, опубликованное через OLE (например, вложение письма или встроенный объект), восстанавливается как набор сырых форматов и теряет связь с объектом исходного приложения.

Если постобработка успешна, вставляется текст постобработки. Если постобработка отключена, вставляется текст распознавания.

Если распознавание завершилось ошибкой, итоговый текст не вставляется. Если распознавание успешно, но постобработка завершилась ошибкой, вставляется текст распознавания — пользователь не теряет результат распознавания.

## Сохранение в историю

После обработки приложение сохраняет запись в историю:

- WAV-файл записи;
- длительность аудио;
- время создания;
- результат распознавания;
- результат постобработки или статус `skipped`;
- итоговый текст;
- статусы этапов;
- провайдера и модель каждого этапа;
- длительность запросов;
- стоимость и usage, если провайдер вернул такие данные;
- снимок настроек обработки на момент запуска этапа.

Запись истории создается также при ошибке распознавания или постобработки, если для этого этапа уже есть достаточно данных.
