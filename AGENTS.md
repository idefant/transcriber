# Project Notes

- Use `lucide-react` icon exports with the `Icon` suffix, for example `CameraIcon`, not `Camera`.
- Follow the component layout rules in [docs/component-structure.md](docs/component-structure.md).
- After finishing changes, follow [docs/after-work-checks.md](docs/after-work-checks.md).
